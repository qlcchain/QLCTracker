import { SqueezeNumberPipe } from './squeeze-number.pipe';

describe('SqueezeNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SqueezeNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
